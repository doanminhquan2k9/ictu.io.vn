<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dịch Vụ Ngừng Hoạt Động</title>
    <style>
        /* Thiết lập nền và toàn bộ trang */
        body {
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #ff9a8b, #ff6a88, #ff6188);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
        }

        /* Container với hiệu ứng và bóng đổ */
        .container {
            background-color: rgba(255, 255, 255, 0.9);
            padding: 40px;
            border-radius: 25px;
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.15);
            width: 80%;
            max-width: 600px;
            text-align: center;
            transform: scale(0.9);
            animation: slideUp 1s ease-out forwards;
            backdrop-filter: blur(10px);
        }

        /* Hiệu ứng trượt lên cho container */
        @keyframes slideUp {
            0% {
                transform: scale(0.5) translateY(100px);
                opacity: 0;
            }
            100% {
                transform: scale(1) translateY(0);
                opacity: 1;
            }
        }

        /* Biểu tượng cảnh báo (chuyển động nhẹ) */
        .icon {
            font-size: 80px;
            color: #ff6a88;
            margin-bottom: 20px;
            animation: float 2s ease-in-out infinite;
        }

        /* Hiệu ứng nổi lên (floating) cho biểu tượng */
        @keyframes float {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-15px);
            }
        }

        /* Tiêu đề chính */
        h1 {
            font-size: 36px;
            color: #333;
            font-weight: 600;
            margin-bottom: 15px;
        }

        /* Mô tả ngừng hoạt động */
        p {
            font-size: 18px;
            color: #555;
            margin-bottom: 30px;
            line-height: 1.6;
            opacity: 0.9;
        }

        /* Nút bấm với hiệu ứng */
        .btn {
            padding: 15px 40px;
            background: linear-gradient(135deg, #ff9a8b, #ff6a88);
            color: white;
            border: none;
            border-radius: 50px;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        /* Hiệu ứng hover cho nút */
        .btn:hover {
            transform: scale(1.05);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.3);
        }

        /* Hiệu ứng focus cho nút */
        .btn:focus {
            outline: none;
            box-shadow: 0 0 10px rgba(255, 105, 180, 0.7);
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="icon">⚠️</div>
        <h1>Dịch Vụ Đang Ngừng Hoạt Động</h1>
        <p>Chúng tôi đang bảo trì dịch vụ để nâng cấp hệ thống. Vui lòng thử lại sau ít phút!</p>
        <button class="btn" onclick="window.location.reload();">Tải Lại Trang</button>
    </div>
</body>
</html>
